import { NoisedSynth } from "@/features/noised/components/noised-synth";

export const metadata = {
    title: "NoiseD Sound Designer | David Portal Dev",
    description: "Design a rain and thunder soundscape with NoiseD.",
};

export default function NoisedDemoPage() {
    return <NoisedSynth />;
}
